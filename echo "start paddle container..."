echo "start paddle container..."

docker run -itd  \
                -p 80:80 \
                --name paddle \
                --hostname paddle \
                -v /Users/steven/Develop/code/pycharm/paddle:/work \
                docker.paddlepaddlehub.com/paddle:latest /bin/sh &> /dev/null

docker exec -it paddle bash